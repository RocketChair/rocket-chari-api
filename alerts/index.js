const config = require('../config')

const alerts = {
    GET_UP_FROM_CHAIR: {
        type: 'ALERT',
        message: 'Move your ass!'
    },
    GET_BACK_TO_WORK: {
        type: 'ALERT',
        message: 'Get back to work!'
    }
}

module.exports = alerts